NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# For secure server-side storage operations
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_VIDEO_BUCKET=encrypted-videos
NEXT_PUBLIC_SUPABASE_VIDEO_BUCKET=encrypted-videos

# Browser-side KDF salt (not secret). Optional override.
NEXT_PUBLIC_VIDEO_KDF_SALT=openclaw-video-salt

# Legacy server decrypt key (optional if using browser-only decrypt flow)
VIDEO_ENCRYPTION_SECRET=
